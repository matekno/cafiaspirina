{
    "commands": [
        {
            "endpoint": "docker-ps",
            "cmd": "docker ps"
        },
        {
            "endpoint": "hello",
            "cmd": "echo 'hello world'"
        }
    ]
}